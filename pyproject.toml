[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "decision-analysis"
version = "0.1.0"
description = "Organizational decision analysis simulation framework"
requires-python = ">=3.9"
